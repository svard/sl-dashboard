(ns net.svard.sl-dashboard.core-spec
  (:require [speclj.core :refer :all]
            [net.svard.sl-dashboard.core :refer :all]))

(comment
  (describe "a test"
    (it "FIXME, I fail."
      (should= 0 1))))
